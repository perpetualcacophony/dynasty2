[package]
name = "dynasty2"
version = "0.1.0"
edition = "2021"

[dependencies]
futures = "0.3.30"
reqwest = { version = "0.12", features = ["json"], optional = true }
serde = { version = "1.0.206", features = ["derive"] }
url = "2.5.2"

[features]
default = ["reqwest"]
reqwest = ["dep:reqwest"]
